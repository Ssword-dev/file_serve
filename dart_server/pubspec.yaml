name: dart_server
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  uuid: ^4.5.1
  mime: ^2.0.0

  # all i need from the async package is extra streaming utils

  async: ">=2.12.0 <3.0.0"
dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0